TODO list:
===========
- RDFSプロパティのdomainとrangeのコピー，カットの実装．
- デフォルトの名前空間を指定できるようにする．
- 名前空間とノードのIDを別々に編集するようにする．
- (Property EditorのRegionの定義を継承できるようにする．)

α2のTODO
- プロジェクト単位で保存できるようにする．(位置情報も保存)
- RDF Editorでラベル，コメントを可能にする．rdfs:labelプロパティで
  指定したものと同期をとるようにする．
- RDF EditorでInsert Resourceを実行したときの入力ダイアログで，
  プルダウンメニューで，タイプを選択できるようにする．
- Select Resource Typeダイアログでクラスの名前で検索を可能にする．
- プロジェクト単位でデフォルトのエンコーディングを指定できるようにする．
- ファイル出力したとき、NameSpaceを設定している場合は、その短縮形で
　出力できて欲しい。

?- RDF EditorでInsert Connected Resource(またはLiteral)とすると、プロパティで
　　つながったリソース(またはリテラル)を追加できるとうれしい。
　⇒矢印の向きは、矢印の先になるものが追加されるのが自然でしょうね。

BugList
- ファイル出力時のエンコーディングがnullになっている。
　⇒言語に応じて選べるとうれしい。コメントとラベルのLangに
　　対応させるとか。

- N-Triple形式でExportまたはConvertすると日本語が文字化けする。
　⇒こんな感じ
　　<\u6B6F> <http://example#\u95A2\u9023>
　　<\u30AD\u30B7\u30EA\u30C8\u30FC\u30EB> .

- クラス選択ダイアログで、矩形サイズを変更すると、Class Editorのノードの
　　矩形サイズも変わってしまう。AttributeダイアログでApplyを押したとき・・・
　⇒仕様なのかも。

- IsaVizで作成したRDFファイルが開けないことがある。
　⇒そもそもIsaVizのほうの問題なのかも知れませんが、IsaVizではエンコーディング
   を指定せずに出力しているため、例えば秀丸のデフォルト設定で開こうとすると、
   おそらくShift_JISだと思って文字化けしてしまいます。
   同様にMR^3側で文字コードがわからずにShiftJISで開こうとしている
   感じがします。ちなみにIsaVizを使ってRDF形式で出力したファイル
   (UTF-8形式)をShiftJISに変換してからMR^3で読み込ませると正常に
   読み込ませることができました。
　　逆にShift_JISにしてしまうとIsaVizでエラーを起こします。
　⇒これはIsaViz1.2および2.0アルファの両方で起きます。
　サンプルファイル）
　　○UTF-8形式(IsaVizでExportしたRDF)
　　　：dental(UTF-8).rdf
　　○ShiftJIS形式(IsaVizでExportしたRDFを文字コードコンバートしたもの)
　　　：dental(Shift_JIS).rdf

- スクロールしても表示できなくなることがある。
　⇒どんな場合に起こるかはまだよくわかりません。

